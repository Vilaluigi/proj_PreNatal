function voltar(caminho) {
  window.electronAPI.voltarTela(caminho);
}
function clicar(caminho) {
  window.electronAPI.trocarTela(caminho);
}
function proxima(caminho) {
  window.electronAPI.trocarTela(caminho);
}
